<Window x:Class="VMS.VisionSetup.Views.Templates.TemplateGalleryWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:shell="clr-namespace:System.Windows.Shell;assembly=PresentationFramework"
        xmlns:c="clr-namespace:VMS.VisionSetup.Views.Common"
        xmlns:vm="clr-namespace:VMS.VisionSetup.ViewModels"
        Title="예제 템플릿"
        Width="860" Height="620"
        MinWidth="720" MinHeight="480"
        WindowStartupLocation="CenterOwner"
        WindowStyle="None"
        Background="{StaticResource BrushBgWindow}"
        TextOptions.TextFormattingMode="Display">

    <!-- 앱 공통 다크 크롬 — 기본 OS 타이틀바(밝은 창틀)로 떠서 테마와 어긋나던 현장 보고 2026-08-28 -->
    <shell:WindowChrome.WindowChrome>
        <shell:WindowChrome CaptionHeight="42" CornerRadius="0"
                            GlassFrameThickness="0" ResizeBorderThickness="6"
                            UseAeroCaptionButtons="False"/>
    </shell:WindowChrome.WindowChrome>

    <Window.Resources>
        <Style TargetType="TextBlock">
            <Setter Property="Foreground" Value="#E6E6E6"/>
        </Style>
        <Style x:Key="DialogButton" TargetType="Button">
            <Setter Property="MinWidth" Value="88"/>
            <Setter Property="Height" Value="30"/>
            <Setter Property="Margin" Value="8,0,0,0"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Background" Value="#3E3E42"/>
            <Setter Property="BorderBrush" Value="#555555"/>
        </Style>

        <!-- 다크 테마 탭 헤더 — 기본 TabItem 크롬(밝은 배경)에서는 밝은 글자가 안 보임 -->
        <Style TargetType="TabItem">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TabItem">
                        <Border x:Name="Bd" Background="#2D2D30"
                                BorderBrush="#3F3F46" BorderThickness="1,1,1,0"
                                CornerRadius="4,4,0,0" Margin="0,0,4,0"
                                Padding="16,8,16,7">
                            <ContentPresenter ContentSource="Header"
                                              HorizontalAlignment="Center"
                                              VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="#3E3E42"/>
                            </Trigger>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="#007ACC"/>
                                <Setter TargetName="Bd" Property="BorderBrush" Value="#007ACC"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Border Background="{StaticResource BrushBgWindow}"
            BorderBrush="{StaticResource BrushBorderSubtle}" BorderThickness="1">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="42"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <c:ChromelessTitleBar Grid.Row="0" TitleText="예제 템플릿"/>

            <Grid Grid.Row="1" Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Margin="2,0,2,10" TextWrapping="Wrap"
                   Foreground="#C8C8C8" FontSize="12"
                   Text="템플릿을 선택하고 [생성]을 누르면 현재 스텝의 워크스페이스에 툴 체인이 만들어집니다 (다중 스텝 템플릿은 레시피에 스텝이 새로 생성됩니다). 생성 후 이미지(또는 3D 데이터)를 로드하고 Run(F5)으로 테스트하세요."/>

        <TabControl Grid.Row="1"
                    ItemsSource="{Binding Categories}"
                    Background="Transparent" BorderBrush="#3F3F46">
            <TabControl.ItemTemplate>
                <DataTemplate>
                    <TextBlock Text="{Binding Name}" Foreground="#F0F0F0"
                               FontSize="13" FontWeight="SemiBold"/>
                </DataTemplate>
            </TabControl.ItemTemplate>
            <TabControl.ContentTemplate>
                <DataTemplate>
                    <ListBox ItemsSource="{Binding Templates}"
                             SelectedItem="{Binding DataContext.SelectedTemplate,
                                            RelativeSource={RelativeSource AncestorType=Window},
                                            Mode=TwoWay}"
                             Background="#252526" BorderThickness="0"
                             ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                             MouseDoubleClick="TemplateList_MouseDoubleClick">
                        <ListBox.ItemsPanel>
                            <ItemsPanelTemplate>
                                <WrapPanel/>
                            </ItemsPanelTemplate>
                        </ListBox.ItemsPanel>
                        <ListBox.ItemContainerStyle>
                            <!-- 기본 ListBoxItem 선택 하이라이트(밝은 파랑)는 밝은 글자를 지움 — 다크 카드 템플릿으로 대체 -->
                            <Style TargetType="ListBoxItem">
                                <Setter Property="Margin" Value="8"/>
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="ListBoxItem">
                                            <Border x:Name="Card" Background="#2D2D30"
                                                    BorderBrush="#3F3F46" BorderThickness="1"
                                                    CornerRadius="4">
                                                <ContentPresenter/>
                                            </Border>
                                            <ControlTemplate.Triggers>
                                                <Trigger Property="IsMouseOver" Value="True">
                                                    <Setter TargetName="Card" Property="Background" Value="#37373B"/>
                                                    <Setter TargetName="Card" Property="BorderBrush" Value="#5A5A5E"/>
                                                </Trigger>
                                                <Trigger Property="IsSelected" Value="True">
                                                    <Setter TargetName="Card" Property="Background" Value="#0E3A5C"/>
                                                    <Setter TargetName="Card" Property="BorderBrush" Value="#3399FF"/>
                                                </Trigger>
                                            </ControlTemplate.Triggers>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                            </Style>
                        </ListBox.ItemContainerStyle>
                        <ListBox.ItemTemplate>
                            <DataTemplate>
                                <StackPanel Width="370" Margin="12">
                                    <Image Source="{Binding DiagramUri}" Height="96"
                                           Stretch="Uniform" HorizontalAlignment="Left"/>
                                    <TextBlock Text="{Binding Title}" FontWeight="Bold"
                                               FontSize="15" Foreground="White" Margin="0,10,0,5"/>
                                    <ItemsControl ItemsSource="{Binding Prerequisites}" Margin="0,0,0,5">
                                        <ItemsControl.ItemsPanel>
                                            <ItemsPanelTemplate>
                                                <WrapPanel/>
                                            </ItemsPanelTemplate>
                                        </ItemsControl.ItemsPanel>
                                        <ItemsControl.ItemTemplate>
                                            <DataTemplate>
                                                <Border Background="#5A3E1B" CornerRadius="3"
                                                        Padding="6,2" Margin="0,0,6,2">
                                                    <TextBlock Text="{Binding}" FontSize="11"
                                                               Foreground="#FFDFAE"/>
                                                </Border>
                                            </DataTemplate>
                                        </ItemsControl.ItemTemplate>
                                    </ItemsControl>
                                    <TextBlock Text="{Binding Description}" TextWrapping="Wrap"
                                               FontSize="12" Foreground="#CFCFCF"
                                               LineHeight="18"/>
                                </StackPanel>
                            </DataTemplate>
                        </ListBox.ItemTemplate>
                    </ListBox>
                </DataTemplate>
            </TabControl.ContentTemplate>
        </TabControl>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,12,0,0">
            <Button Content="생성" Style="{StaticResource DialogButton}"
                    Background="#007ACC" BorderBrush="#007ACC"
                    Command="{Binding CreateCommand}" IsDefault="True"/>
            <Button Content="취소" Style="{StaticResource DialogButton}"
                    Command="{Binding CancelCommand}" IsCancel="True"/>
        </StackPanel>
            </Grid>
        </Grid>
    </Border>
</Window>
