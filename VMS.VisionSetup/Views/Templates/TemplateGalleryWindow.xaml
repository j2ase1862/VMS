<Window x:Class="VMS.VisionSetup.Views.Templates.TemplateGalleryWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:vm="clr-namespace:VMS.VisionSetup.ViewModels"
        Title="예제 템플릿"
        Width="860" Height="620"
        MinWidth="720" MinHeight="480"
        WindowStartupLocation="CenterOwner"
        Background="#1E1E1E">
    <Window.Resources>
        <Style TargetType="TextBlock">
            <Setter Property="Foreground" Value="#E6E6E6"/>
        </Style>
        <Style x:Key="DialogButton" TargetType="Button">
            <Setter Property="MinWidth" Value="88"/>
            <Setter Property="Height" Value="30"/>
            <Setter Property="Margin" Value="8,0,0,0"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Background" Value="#3E3E42"/>
            <Setter Property="BorderBrush" Value="#555555"/>
        </Style>
    </Window.Resources>

    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Margin="2,0,2,10" TextWrapping="Wrap" Foreground="#B0B0B0"
                   Text="템플릿을 선택하고 [생성]을 누르면 현재 스텝의 워크스페이스에 툴 체인이 만들어집니다. 생성 후 이미지(또는 3D 데이터)를 로드하고 Run(F5)으로 테스트하세요."/>

        <TabControl Grid.Row="1"
                    ItemsSource="{Binding Categories}"
                    Background="Transparent" BorderBrush="#3F3F46">
            <TabControl.ItemTemplate>
                <DataTemplate>
                    <TextBlock Text="{Binding Name}" Foreground="#E6E6E6" Margin="6,2"/>
                </DataTemplate>
            </TabControl.ItemTemplate>
            <TabControl.ContentTemplate>
                <DataTemplate>
                    <ListBox ItemsSource="{Binding Templates}"
                             SelectedItem="{Binding DataContext.SelectedTemplate,
                                            RelativeSource={RelativeSource AncestorType=Window},
                                            Mode=TwoWay}"
                             Background="#252526" BorderThickness="0"
                             ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                             MouseDoubleClick="TemplateList_MouseDoubleClick">
                        <ListBox.ItemsPanel>
                            <ItemsPanelTemplate>
                                <WrapPanel/>
                            </ItemsPanelTemplate>
                        </ListBox.ItemsPanel>
                        <ListBox.ItemContainerStyle>
                            <Style TargetType="ListBoxItem">
                                <Setter Property="Margin" Value="8"/>
                                <Setter Property="Padding" Value="0"/>
                                <Setter Property="Background" Value="#2D2D30"/>
                                <Setter Property="BorderBrush" Value="#3F3F46"/>
                                <Setter Property="BorderThickness" Value="1"/>
                                <Style.Triggers>
                                    <Trigger Property="IsSelected" Value="True">
                                        <Setter Property="BorderBrush" Value="#007ACC"/>
                                        <Setter Property="BorderThickness" Value="2"/>
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </ListBox.ItemContainerStyle>
                        <ListBox.ItemTemplate>
                            <DataTemplate>
                                <StackPanel Width="370" Margin="10">
                                    <Image Source="{Binding DiagramUri}" Height="96"
                                           Stretch="Uniform" HorizontalAlignment="Left"/>
                                    <TextBlock Text="{Binding Title}" FontWeight="Bold"
                                               FontSize="14" Margin="0,8,0,4"/>
                                    <ItemsControl ItemsSource="{Binding Prerequisites}" Margin="0,0,0,4">
                                        <ItemsControl.ItemsPanel>
                                            <ItemsPanelTemplate>
                                                <WrapPanel/>
                                            </ItemsPanelTemplate>
                                        </ItemsControl.ItemsPanel>
                                        <ItemsControl.ItemTemplate>
                                            <DataTemplate>
                                                <Border Background="#5A3E1B" CornerRadius="3"
                                                        Padding="6,2" Margin="0,0,6,2">
                                                    <TextBlock Text="{Binding}" FontSize="11"
                                                               Foreground="#FFD9A0"/>
                                                </Border>
                                            </DataTemplate>
                                        </ItemsControl.ItemTemplate>
                                    </ItemsControl>
                                    <TextBlock Text="{Binding Description}" TextWrapping="Wrap"
                                               FontSize="12" Foreground="#B8B8B8"/>
                                </StackPanel>
                            </DataTemplate>
                        </ListBox.ItemTemplate>
                    </ListBox>
                </DataTemplate>
            </TabControl.ContentTemplate>
        </TabControl>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,12,0,0">
            <Button Content="생성" Style="{StaticResource DialogButton}"
                    Background="#007ACC" BorderBrush="#007ACC"
                    Command="{Binding CreateCommand}" IsDefault="True"/>
            <Button Content="취소" Style="{StaticResource DialogButton}"
                    Command="{Binding CancelCommand}" IsCancel="True"/>
        </StackPanel>
    </Grid>
</Window>
