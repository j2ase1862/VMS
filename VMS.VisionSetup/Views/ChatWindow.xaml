<Window x:Class="VMS.VisionSetup.Views.ChatWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:shell="clr-namespace:System.Windows.Shell;assembly=PresentationFramework"
        xmlns:c="clr-namespace:VMS.VisionSetup.Views.Common"
        xmlns:converters="clr-namespace:VMS.VisionSetup.Converters"
        Title="SLM Recipe Bot"
        Width="600" Height="720"
        MinWidth="440" MinHeight="520"
        WindowStyle="None"
        Background="{StaticResource BrushBgWindow}"
        WindowStartupLocation="CenterScreen"
        TextOptions.TextFormattingMode="Display">

    <shell:WindowChrome.WindowChrome>
        <shell:WindowChrome CaptionHeight="42" CornerRadius="0"
                            GlassFrameThickness="0" ResizeBorderThickness="6"
                            UseAeroCaptionButtons="False"/>
    </shell:WindowChrome.WindowChrome>

    <Window.Resources>
        <converters:BoolToAlignmentConverter x:Key="BoolToAlignmentConverter"/>
        <converters:InverseBoolConverter x:Key="InverseBoolConverter"/>
        <BooleanToVisibilityConverter x:Key="BoolToVisibilityConverter"/>

        <!-- Chat-specific Send/Apply/Reset buttons (preserve original semantics) -->
        <Style x:Key="ChatButtonBase" TargetType="Button">
            <Setter Property="FontSize" Value="11.5"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="14,7"/>
            <Setter Property="SnapsToDevicePixels" Value="True"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border"
                                Background="{TemplateBinding Background}"
                                CornerRadius="6"
                                Padding="{TemplateBinding Padding}"
                                SnapsToDevicePixels="True">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Opacity" Value="0.88"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="Opacity" Value="0.72"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="border" Property="Opacity" Value="0.35"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="SendButtonStyle" TargetType="Button" BasedOn="{StaticResource ChatButtonBase}">
            <Setter Property="Background" Value="{StaticResource BrushAccent}"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border"
                                Background="{TemplateBinding Background}"
                                CornerRadius="6"
                                Padding="12,7">
                            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                <Path Data="M2,21L23,12L2,3V10L17,12L2,14V21Z"
                                      Fill="White" Stretch="Uniform" Width="13" Height="13"
                                      VerticalAlignment="Center" Margin="0,0,5,0"/>
                                <TextBlock Text="Send" Foreground="White" FontSize="12" FontWeight="SemiBold"
                                           VerticalAlignment="Center"/>
                            </StackPanel>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="{StaticResource BrushAccentHover}"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="border" Property="Opacity" Value="0.35"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="ApplyButtonStyle" TargetType="Button" BasedOn="{StaticResource ChatButtonBase}">
            <Setter Property="Background" Value="{StaticResource BrushSuccess}"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" Background="{TemplateBinding Background}"
                                CornerRadius="6" Padding="12,7">
                            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                <Path Data="M21,7L9,19L3.5,13.5L4.91,12.09L9,16.17L19.59,5.59L21,7Z"
                                      Fill="White" Stretch="Uniform" Width="12" Height="12"
                                      VerticalAlignment="Center" Margin="0,0,5,0"/>
                                <TextBlock Text="Apply Recipe" Foreground="White" FontSize="11.5" FontWeight="SemiBold"
                                           VerticalAlignment="Center"/>
                            </StackPanel>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Opacity" Value="0.88"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="border" Property="Opacity" Value="0.35"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="ResetButtonStyle" TargetType="Button" BasedOn="{StaticResource ChatButtonBase}">
            <Setter Property="Background" Value="{StaticResource BrushBgSurface}"/>
            <Setter Property="Foreground" Value="{StaticResource BrushTextSecondary}"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" Background="{TemplateBinding Background}"
                                CornerRadius="6" Padding="12,7"
                                BorderBrush="{StaticResource BrushBorderSubtle}" BorderThickness="1">
                            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                <Path Data="M17.65,6.35C16.2,4.9 14.21,4 12,4A8,8 0 0,0 4,12A8,8 0 0,0 12,20C15.73,20 18.84,17.45 19.73,14H17.65C16.83,16.33 14.61,18 12,18A6,6 0 0,1 6,12A6,6 0 0,1 12,6C13.66,6 15.14,6.69 16.22,7.78L13,11H20V4L17.65,6.35Z"
                                      Fill="{StaticResource BrushTextSecondary}" Stretch="Uniform" Width="12" Height="12"
                                      VerticalAlignment="Center" Margin="0,0,5,0"/>
                                <TextBlock Text="Reset" Foreground="{StaticResource BrushTextSecondary}"
                                           FontSize="11.5" VerticalAlignment="Center"/>
                            </StackPanel>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="{StaticResource BrushBgHover}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Toggle Switch (자동 실행 / Operator Mode) -->
        <Style x:Key="ToggleSwitchStyle" TargetType="CheckBox">
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="CheckBox">
                        <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                            <Grid Width="32" Height="18">
                                <Border x:Name="track" Background="{StaticResource BrushBgInput}"
                                        BorderBrush="{StaticResource BrushBorderStrong}" BorderThickness="1"
                                        CornerRadius="9"/>
                                <Border x:Name="thumb" Width="14" Height="14" CornerRadius="7"
                                        Background="{StaticResource BrushTextMuted}"
                                        HorizontalAlignment="Left" Margin="2,0,0,0"/>
                            </Grid>
                            <TextBlock x:Name="label" Text="{TemplateBinding Content}"
                                       Foreground="{StaticResource BrushTextMuted}"
                                       FontSize="11" VerticalAlignment="Center" Margin="7,0,0,0"/>
                        </StackPanel>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="track" Property="Background" Value="{StaticResource BrushAccent}"/>
                                <Setter TargetName="track" Property="BorderBrush" Value="{StaticResource BrushAccent}"/>
                                <Setter TargetName="thumb" Property="Background" Value="White"/>
                                <Setter TargetName="thumb" Property="HorizontalAlignment" Value="Right"/>
                                <Setter TargetName="thumb" Property="Margin" Value="0,0,2,0"/>
                                <Setter TargetName="label" Property="Foreground" Value="{StaticResource BrushTextPrimary}"/>
                                <Setter TargetName="label" Property="FontWeight" Value="SemiBold"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Border Background="{StaticResource BrushBgWindow}"
            BorderBrush="{StaticResource BrushBorderSubtle}" BorderThickness="1">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="42"/>   <!-- Title bar -->
                <RowDefinition Height="Auto"/> <!-- Toggle toolbar -->
                <RowDefinition Height="Auto"/> <!-- Model row -->
                <RowDefinition Height="*"/>    <!-- Messages -->
                <RowDefinition Height="Auto"/> <!-- Input -->
                <RowDefinition Height="26"/>   <!-- Status bar -->
            </Grid.RowDefinitions>

            <c:ChromelessTitleBar Grid.Row="0" TitleText="SLM Recipe Bot"/>

            <!-- Toggle toolbar (자동 실행 / Operator Mode) -->
            <Border Grid.Row="1" Background="{StaticResource BrushBgTitleBar}"
                    BorderBrush="{StaticResource BrushBorderSubtle}"
                    BorderThickness="0,0,0,1" Padding="14,8">
                <StackPanel Orientation="Horizontal">
                    <CheckBox Content="자동 실행"
                              IsChecked="{Binding IsAutoRunEnabled}"
                              Style="{StaticResource ToggleSwitchStyle}"
                              VerticalAlignment="Center" Margin="0,0,18,0"
                              ToolTip="레시피 Apply 후 자동으로 도구 실행 + 결과 메시지 추가"/>
                    <CheckBox Content="Operator Mode"
                              IsChecked="{Binding IsOperatorMode}"
                              Style="{StaticResource ToggleSwitchStyle}"
                              VerticalAlignment="Center"/>
                </StackPanel>
            </Border>

            <!-- Model row -->
            <Border Grid.Row="2" Background="{StaticResource BrushBgCard}"
                    BorderBrush="{StaticResource BrushBorderSubtle}"
                    BorderThickness="0,0,0,1" Padding="14,8">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock Grid.Column="0" Text="Model"
                               Foreground="{StaticResource BrushTextMuted}" FontSize="11"
                               VerticalAlignment="Center" Margin="0,0,8,0"/>
                    <TextBox Grid.Column="1"
                             Text="{Binding ModelName, UpdateSourceTrigger=PropertyChanged}"
                             Style="{StaticResource WindowDarkTextBox}"
                             IsEnabled="{Binding IsBusy, Converter={StaticResource InverseBoolConverter}}"/>
                    <Button Grid.Column="2" Content="Connect"
                            Command="{Binding ConnectModelCommand}"
                            Style="{StaticResource ModernPrimaryButton}"
                            Width="72" Margin="8,0,0,0"/>
                </Grid>
            </Border>

            <!-- Chat Messages -->
            <ScrollViewer Grid.Row="3" x:Name="ChatScrollViewer"
                          VerticalScrollBarVisibility="Auto"
                          Background="{StaticResource BrushBgWindow}" Padding="10">
                <ItemsControl ItemsSource="{Binding Messages}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border Margin="4"
                                    Padding="11,9"
                                    CornerRadius="10"
                                    MaxWidth="440"
                                    HorizontalAlignment="{Binding IsUser, Converter={StaticResource BoolToAlignmentConverter}}">
                                <Border.Style>
                                    <Style TargetType="Border">
                                        <Setter Property="Background" Value="{StaticResource BrushBgSurface}"/>
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding IsUser}" Value="True">
                                                <Setter Property="Background" Value="{StaticResource BrushAccent}"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </Border.Style>
                                <TextBlock Text="{Binding Content}" Foreground="{StaticResource BrushTextPrimary}"
                                           TextWrapping="Wrap" FontSize="12.5"/>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </ScrollViewer>

            <!-- Input -->
            <Border Grid.Row="4" Background="{StaticResource BrushBgTitleBar}"
                    BorderBrush="{StaticResource BrushBorderSubtle}"
                    BorderThickness="0,1,0,0" Padding="10,10">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <Grid Grid.Row="0">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <TextBox Grid.Column="0" x:Name="InputTextBox"
                                 Text="{Binding UserInput, UpdateSourceTrigger=PropertyChanged}"
                                 Style="{StaticResource WindowDarkTextBox}"
                                 FontSize="12.5"
                                 AcceptsReturn="False"
                                 KeyDown="InputTextBox_KeyDown"
                                 IsEnabled="{Binding IsBusy, Converter={StaticResource InverseBoolConverter}}"/>
                        <Button Grid.Column="1" Command="{Binding SendMessageCommand}"
                                Style="{StaticResource SendButtonStyle}" Margin="8,0,0,0"/>
                    </Grid>

                    <Grid Grid.Row="1" Margin="0,8,0,0">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <Button Grid.Column="0" Command="{Binding ApplyRecipeCommand}"
                                Style="{StaticResource ApplyButtonStyle}" Margin="0,0,4,0"/>
                        <Button Grid.Column="1" Command="{Binding ResetSessionCommand}"
                                Style="{StaticResource ResetButtonStyle}" Margin="4,0,0,0"/>
                    </Grid>
                </Grid>
            </Border>

            <!-- Status Bar -->
            <Border Grid.Row="5" Background="{StaticResource BrushAccent}" Padding="10,2">
                <Grid>
                    <TextBlock Text="{Binding StatusMessage}" Foreground="White" FontSize="11"
                               VerticalAlignment="Center" TextTrimming="CharacterEllipsis"/>
                    <ProgressBar IsIndeterminate="{Binding IsBusy}" Height="2"
                                 VerticalAlignment="Bottom" Background="Transparent"
                                 Foreground="White" BorderThickness="0"
                                 Visibility="{Binding IsBusy, Converter={StaticResource BoolToVisibilityConverter}}"/>
                </Grid>
            </Border>
        </Grid>
    </Border>
</Window>
