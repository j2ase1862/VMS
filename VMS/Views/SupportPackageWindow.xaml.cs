using System.Windows;

namespace VMS.Views
{
    /// <summary>
    /// 지원 패키지 export 윈도우 — Admin 권한 전용.
    /// DataContext 는 SupportPackageViewModel.
    /// </summary>
    public partial class SupportPackageWindow : Window
    {
        public SupportPackageWindow()
        {
            InitializeComponent();
        }
    }
}
